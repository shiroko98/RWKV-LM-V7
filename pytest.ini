[pytest]
testpaths = tests
norecursedirs = .git .torch_extensions outs _reference_rwkv_infctx_trainer
addopts = -ra
markers =
    cuda: optional CUDA integration tests
    slow: slow integration tests

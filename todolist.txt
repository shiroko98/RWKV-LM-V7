2026-06-10 Current SFT open tasks

Current status
- SFT preprocessing core path is implemented: UTF-8 JSONL input, recursive directory input, multiprocessing render/tokenize, progress display, best-fit-decreasing packing, bounded shard groups, group cache/resume, error log, trailing tool-result trimming, and aligned token/mask binidx output.
- SFT training core path is implemented: `data_type=sft_binidx`, automatic mask sidecar loading, shifted next-token loss mask, masked CE, explicit SFT epoch schedule, `--sft_one_pass`, gradient accumulation, WSD final LR decay, DeepSpeed resume, ZeRO checkpoint conversion, and chat-template-rendered converted-checkpoint demo.
- README / README_CN document that SFT preprocessing worker processes run concurrently while progress is reported in input-order result-consumption order.
- Added a self-contained ctx86016 one-pass 13.3B SFT launcher with conservative long-run defaults; SFT does not need `MAGIC_PRIME`.
- Added switchable SFT chunked masked-head CE for experiments, but server feedback shows positive chunks still timeout under 13B ZeRO-3 long-context SFT; production launchers currently keep `sft_masked_ce_chunk=0`.
- Added an optional server CUDA equivalence/perf smoke for chunked masked-head CE vs full-logits CE: `RWKV_RUN_CUDA_SFT_MASKED_CE_CHUNK_EQUIV_SMOKE=1`; keep it as a small-model / single-GPU comparison, not as the production 13B ZeRO-3 path.
- Added `run_13b_sft_profile.sh` for short 13.3B SFT profiling runs. It records train logs, GPU/CPU sampling, optional NCCL logs, and optional Nsight Systems traces for diagnosing CUDA kernel time versus ZeRO/offload/communication waits.
- Added DeepSpeed ZeRO-3 tuning switches for SFT profiling/13B runs: offload `pin_memory`, stage3 parameter persistence threshold, prefetch bucket size, and max live parameters.
- Added a strict CUDA op-level precision smoke for `rwkv7_head_l2wrap_ce_bf16_v4.forward_masked`: BF16, vocab 65536, hidden 4096, multiple chunks, and all-active/sparse/single-token/zero masks.
- The historical entries below are kept as an append-only implementation log. Some older `Next` or `TODO` items are already completed by later dated entries.

Open tasks
- Run the full production SFT preprocessing job on the server with the chosen `--pack-shard-group-size`, `--num-workers`, `--worker-chunksize`, `--pack-cache-dir`, `--error-log`, and `--progress` settings.
- After preprocessing completes, inspect the error/info log: real errors should be fixed or filtered intentionally; `label=trim_trailing_tool_messages` entries should be counted and spot-checked.
- Validate the final production binidx pair: token and mask document counts match, document lengths match the training `ctx_len + 1`, mask values are only `0/1`, packing/padding behavior is expected, and mask density looks reasonable.
- Decide final production preprocessing parameters, especially `CTX_LEN`, `--pack-shard-group-size`, shuffle policy, and cache location.
- Run a short 13.3B SFT smoke on a small real-data shard to confirm real-data loss, throughput, memory usage, checkpoint writing, and log output.
- Run `run_13b_sft_profile.sh` on the server with the intended 13B config to collect step timing, GPU utilization, NCCL logs, and optional nsys trace before deciding the next CUDA/operator optimization.
- Compare pure `deepspeed_stage_3` against tuned `deepspeed_stage_3_offload` with the new DeepSpeed knobs: `DS_BUCKET_MB`, `DS_OFFLOAD_PIN_MEMORY`, `DS_STAGE3_PARAM_PERSISTENCE_THRESHOLD`, `DS_STAGE3_PREFETCH_BUCKET_SIZE`, and `DS_STAGE3_MAX_LIVE_PARAMETERS`.
- Run a longer 13.3B dry run on 8xH800 with the intended DeepSpeed strategy to validate ZeRO/offload stability, activation checkpointing, checkpoint retention, resume, and WSD LR behavior under realistic settings.
- Fill `SAVE_EVERY_N_STEPS` after measuring how many optimizer steps correspond to about half a day, and choose `LR_WSD_DECAY_ITERS` after confirming the one-pass step count.
- After the first real 13.3B checkpoint, run DeepSpeed/ZeRO shard conversion to single `.pth`, converted-checkpoint equivalence, and chat-template inference smoke.
- Finalize training hyperparameters: `MICRO_BSZ`, `ACCUMULATE_GRAD_BATCHES`, `LR_INIT`, `LR_FINAL`, `LR_WSD_DECAY_ITERS`, `LR_WSD_DECAY_STYLE`, `WEIGHT_DECAY`, checkpoint interval, checkpoint retention, and whether to use `SFT_ONE_PASS=1` or manual `EPOCH_STEPS/EPOCH_COUNT`.
- Optionally push branch `new_sft` after the current local commits and this todolist cleanup are confirmed.

Validation notes
- Latest recorded local preprocessing coverage: `tests/test_sft_binidx.py` passed with total modified-surface coverage at or above 95%.
- Latest recorded SFT training targeted coverage: `D:\anaconda\envs\model\python.exe -m pytest -q tests\test_sft_training.py tests\test_deepspeed_checkpointing.py --cov=train --cov=src.dataset --cov=src.sft_loss --cov=src.lr_schedule --cov-report=term-missing --tb=short` -> 45 passed, total 99%; `src.sft_loss.py` 96%.
- Latest recorded optional masked-head CE chunk equivalence smoke default behavior: `D:\anaconda\envs\model\python.exe -m pytest -q tests\test_sft_cuda_smoke.py::test_cuda_sft_masked_ce_chunk_training_matches_full_logits --tb=short` -> 1 skipped until the CUDA env flag is enabled.
- Latest recorded syntax checks: `D:\anaconda\envs\model\python.exe -m compileall -q train.py src tests`, `bash -n run_13b_sft_zero3_offload.sh`, and `bash -n run_13b_sft_ctx86016_onepass.sh` passed.
- Latest recorded server smoke results include CUDA SFT forward/backward, train.py SFT, resume, DP/ZeRO gradient accumulation equivalence, WSD LR resume, and DeepSpeed checkpoint conversion.
- Latest recorded server fused masked CE smokes passed with the latest config: single-GPU full-logits-vs-fused equivalence and train.py + 8xH800 ZeRO-3-offload fused CE smoke.
- Latest 13.3B nsys profiling showed approximate accumulated GPU-kernel shares: `wkv7/clampw` 38.7%, cuBLASLt GEMM 37.9%, NCCL 11.9%, RWKV pointwise 7.4%, LayerNorm 1.2%, SFT fused masked CE small kernels 0.1%.
- New strict CUDA op-level fused masked CE smoke is available as `RWKV_RUN_CUDA_SFT_MASKED_FUSED_CE_OP_EQUIV_SMOKE=1`; run it on the server and record the summary JSON result.
- Latest DeepSpeed A/B profile favors the lowmem stage3 settings with `DS_BUCKET_MB=64`; bucket-only `128` increased peak VRAM slightly and reduced throughput, while the faster fully tuned group was too close to the 80G VRAM limit for a long default.
- README / README_CN now document which CUDA operators are shared model-body paths (`rwkv7_clampw_v3`, tmix, cmix) and which head/CE paths are pretraining-only or SFT-only.
- 13B launcher defaults now use the measured production recommendation: `SFT_MASKED_FUSED_CE_CHUNK=4096`, `DS_BUCKET_MB=64`, `DS_OFFLOAD_PIN_MEMORY=1`, `DS_STAGE3_PARAM_PERSISTENCE_THRESHOLD=0`, `DS_STAGE3_PREFETCH_BUCKET_SIZE=5000000`, and `DS_STAGE3_MAX_LIVE_PARAMETERS=200000000`.
- Added `scripts/calc_sft_onepass_steps.py` so one-pass SFT `epoch_steps` can be computed directly from `DATA_FILE.idx` without starting a long training run.
- Added SFT tail-split eval design: fixed-step eval can use held-out tail docs or overlap with full training docs, logs eval loss/ppl to wandb, and does not conflict with checkpoint saves at the same step.
- CUDA SFT tail-eval smoke now covers both held-out mode with enough synthetic documents and overlap mode with a tiny one-document dataset.
- SFT eval now calls a dedicated `sft_eval_step` that gathers ZeRO-3 sharded head weights before masked/head CE, while the training path keeps the original non-gathered behavior.
- Latest server feedback for positive `--sft_masked_ce_chunk`: `4096` and `8192` still timeout under 13B ZeRO-3 long-context SFT, so production scripts should keep it at `0` until the fused masked CUDA op exists.
- `git diff -- data/SFT/sample/chat_template.jinja` is empty for the current SFT masked-head CE change.

2026-06-10 SFT profiling launcher

Completed
- Added `run_13b_sft_profile.sh`, a short 13.3B ctx86016 SFT diagnostic launcher.
- The script uses the production SFT mask/fused CE settings by default, runs only `PROFILE_STEPS`, disables wandb/checkpoint saves, and writes `train.log`, `gpu_monitor.csv`, optional `vmstat.log`, optional NCCL logs, and optional Nsight Systems trace/stats to `PROJ_DIR`.
- Documented how to compare `deepspeed_stage_3_offload` and `deepspeed_stage_3`, and how to interpret low GPU utilization, high NCCL time, or model-kernel-heavy traces in README / README_CN.
- Updated the default `NSYS_TRACE` to `cuda,nvtx,osrt,cublas` after server feedback showed this Nsight Systems version rejects `--trace=nccl`; NCCL details remain available through `NCCL_DEBUG_FILE`.
- Added README / README_CN bottleneck tables from the 13.3B nsys run and exposed DeepSpeed ZeRO-3 tuning knobs in `train.py`, `run_13b_sft_ctx86016_onepass.sh`, `run_13b_sft_zero3_offload.sh`, and `run_13b_sft_profile.sh`.

2026-06-10 Disable production SFT masked CE chunking

Completed
- Updated 13B production launchers and README / README_CN to keep `SFT_MASKED_CE_CHUNK=0` for production runs.
- Documented that `0` does not add chunking/recompute/all-gather overhead, but still uses the old full-logits masked CE path and can be memory-heavy.
- Recorded server feedback that positive chunk values such as `4096` and `8192` still timeout under 13B ZeRO-3 long-context training.
- This entry was superseded by the CUDA-only `--sft_masked_fused_ce_chunk` implementation below. The old Python chunked path remains available for small-model / single-GPU comparison, but production should keep `SFT_MASKED_CE_CHUNK=0`.

2026-06-10 SFT masked fused CE CUDA implementation

In progress
- Added CUDA-only `rwkv7_head_l2wrap_ce_bf16_v4.forward_masked` for SFT masked head CE.
- The fused CUDA path takes `hidden`, `head.weight`, `targets`, `loss_mask`, and an internal row chunk size. It computes temporary head logits inside the op, skips `mask=0` rows, normalizes loss/gradients by `loss_mask.sum()`, and returns `grad_hidden` plus `grad_weight`.
- The SFT fused path does not apply L2Wrap, so it is intended to match current SFT masked CE rather than the pretraining loss.
- Pretraining `(x, y)` fused CE is left unchanged.
- HIP files are intentionally untouched for now; the new SFT fused path is CUDA/H800 focused.
- Added `--sft_masked_fused_ce_chunk` and `SFT_MASKED_FUSED_CE_CHUNK`, separate from the old Python `--sft_masked_ce_chunk`.
- Added CUDA smoke coverage for single-GPU full-logits vs fused masked CE equivalence and a train.py + DeepSpeed/ZeRO fused CE smoke for timeout/memory validation.

2026-06-10 SFT masked fused CE investigation

Pending
- Read-only inspection found two existing CE CUDA families:
  `rwkv7_l2wrap_ce_bf16_v2` consumes already-materialized logits, while `rwkv7_head_l2wrap_ce_bf16_v4` fuses head projection and CE over row chunks.
- Adding `loss_mask` only to the logits-based CE is comparatively small, but it does not remove the full `[B,T,V]` logits allocation that caused 13.3B ctx86016 SFT OOM.
- The better target is a masked head fused CE path that computes head logits internally, applies the SFT mask per row, normalizes by trainable-token count, and returns `grad_hidden` plus `grad_weight` from a single fused call.
- Keep the existing pretraining fused CE behavior unchanged; SFT should opt into the new masked fused path only after equivalence and ZeRO-3 smoke tests pass.

2026-06-10 SFT trailing tool trim info log

Completed
- Kept the trailing `tool` trimming behavior from `7457700`.
- Added tagged non-error `--error-log` events for samples whose trailing tool messages are trimmed:
  `event_type=info`, `label=trim_trailing_tool_messages`, source path, line number, trimmed count, and record summary.
- Multiprocessing workers return the tagged info event to the main process; workers still do not write the log directly.
- Updated README error-log description to mention tagged informational trim events.
- Kept `data/SFT/sample/chat_template.jinja` untouched.

Validation completed
- `D:\anaconda\envs\model\python.exe -m pytest -q tests\test_sft_binidx.py::test_build_document_from_record_trims_trailing_tool_results tests\test_sft_binidx.py::test_trim_trailing_tool_messages_keeps_middle_tool_context tests\test_sft_binidx.py::test_build_documents_from_sources_logs_trailing_tool_trim_info --tb=short` -> 3 passed.
- `D:\anaconda\envs\model\python.exe -m pytest -q tests\test_sft_binidx.py --cov=src.sft_binidx --cov=data.make_sft_binidx --cov=data.tokenizer.rwkv_tokenizer --cov-report=term-missing --tb=short` -> 88 passed, total 95%; `src.sft_binidx.py` 95%, `data.make_sft_binidx.py` 98%.
- `D:\anaconda\envs\model\python.exe -m compileall -q src data tests` passed.
- `git diff --check` passed with repository line-ending warnings only.
- `git diff -- data/SFT/sample/chat_template.jinja` was empty.

2026-06-10 SFT trailing tool-result trimming

Completed
- Added preprocessing-side trimming for trailing `tool` messages before SFT rendering.
- Final assistant `tool_calls` remain trainable, but external trailing tool results are not rendered into the training document and cannot enter `loss_mask=1`.
- Middle `tool` messages are preserved as non-trainable context when a later assistant reply is the target.
- Updated README with the final-assistant target contract and trailing-tool behavior.
- Kept `data/SFT/sample/chat_template.jinja` untouched.

Validation completed
- `D:\anaconda\envs\model\python.exe -m pytest -q tests\test_sft_binidx.py::test_build_document_from_record_trains_final_tool_calls_when_content_empty tests\test_sft_binidx.py::test_build_document_from_record_trims_trailing_tool_results tests\test_sft_binidx.py::test_trim_trailing_tool_messages_keeps_middle_tool_context --tb=short` -> 3 passed.
- `D:\anaconda\envs\model\python.exe -m pytest -q tests\test_sft_binidx.py --cov=src.sft_binidx --cov=data.make_sft_binidx --cov=data.tokenizer.rwkv_tokenizer --cov-report=term-missing --tb=short` -> 87 passed, total 95%; `src.sft_binidx.py` 95%, `data.make_sft_binidx.py` 98%.
- `D:\anaconda\envs\model\python.exe -m compileall -q src data tests` passed.
- `git diff --check` passed with repository line-ending warnings only.
- `git diff -- data/SFT/sample/chat_template.jinja` was empty.

2026-06-03 new_sft branch bootstrap

2026-06-09 SFT preprocessing multiprocessing and group cache

Completed
- Replaced SFT preprocessing ThreadPool paths with ProcessPool workers for JSONL reading and render/tokenize CPU work.
- Worker processes initialize tokenizer and chat template once, then process JSONL samples in deterministic order.
- Added `--worker-chunksize` to reduce process scheduling overhead for many short samples.
- Optimized `best-fit-decreasing` bin lookup from repeated full scans to a remaining-capacity index.
- Added `--pack-cache-dir` for `best-fit-decreasing` + `--no-shuffle` group-level cache and resume.
- Progress output now shows stages such as read-jsonl, render-tokenize, cache-hit, and cache-write.
- Updated README / README_CN with recommended 13B preprocessing commands and cache/resume guidance.

Validation completed
- `D:\anaconda\envs\model\python.exe -m pytest -q tests\test_sft_binidx.py --cov=src.sft_binidx --cov=data.make_sft_binidx --cov=data.tokenizer.rwkv_tokenizer --cov-report=term-missing --tb=short` -> 80 passed, total 95%; `src.sft_binidx.py` 95%, `data.make_sft_binidx.py` 97%.
- `D:\anaconda\envs\model\python.exe -m pytest -q tests\test_sft_training.py tests\test_deepspeed_checkpointing.py tests\test_convert_deepspeed_checkpoint_to_pth.py tests\test_run_converted_rwkv_demo.py tests\test_converted_checkpoint_equivalence.py tests\test_sft_prompt.py --tb=short` -> 69 passed.
- `D:\anaconda\envs\model\python.exe -m compileall -q src data tests train.py scripts` passed.
- `D:\anaconda\envs\model\python.exe data\make_sft_binidx.py --help` passed and shows `--pack-cache-dir` plus `--worker-chunksize`.
- `git diff --check` passed with only repository line-ending warnings.
- `git diff -- data/SFT/sample/chat_template.jinja` was empty.

2026-06-09 SFT preprocessing error logging

Completed
- Added optional `--error-log` to `data/make_sft_binidx.py`.
- Error logging is disabled by default and does not skip bad samples; preprocessing still stops on the first error.
- On JSON parse, template render, tokenization, or mask-boundary errors, the main process writes one JSONL event with source path, line number, error type/message, raw source text, parsed full record when available, and a compact record summary.
- Multiprocessing workers carry the exact failing sample back to the main process; worker processes do not write the error log directly.
- Kept `data/SFT/sample/chat_template.jinja` untouched.

Validation completed
- Targeted error-log tests passed locally:
  `D:\anaconda\envs\model\python.exe -m pytest -q tests\test_sft_binidx.py::test_build_documents_from_sources_logs_full_record_on_render_errors tests\test_sft_binidx.py::test_build_documents_from_sources_logs_worker_errors_with_exact_source tests\test_sft_binidx.py::test_cli_main_writes_error_log_with_full_record --tb=short`
- Full preprocessing regression and coverage passed locally:
  `D:\anaconda\envs\model\python.exe -m pytest -q tests\test_sft_binidx.py --cov=src.sft_binidx --cov=data.make_sft_binidx --cov=data.tokenizer.rwkv_tokenizer --cov-report=term-missing --tb=short` -> 84 passed, total 96%; `src.sft_binidx.py` 95%, `data.make_sft_binidx.py` 98%.

2026-06-09 SFT UTF-8 split-token mask regression

Completed
- Fixed character-span projection for UTF-8 characters that tokenizer splits into multiple byte tokens.
- Added a regression with rare math symbol `⋃` in `A⋃_{i=1} B`; the test masks only that character and verifies all byte-split tokens for the symbol are trainable while adjacent tokens stay masked.
- Kept the existing Chinese mask projection test.
- Kept `data/SFT/sample/chat_template.jinja` untouched.

Validation completed
- `D:\anaconda\envs\model\python.exe -m pytest -q tests\test_sft_binidx.py::test_tokenize_with_char_spans_and_mask_projection tests\test_sft_binidx.py::test_tokenize_with_char_spans_masks_all_byte_tokens_for_split_utf8_char --tb=short` -> 2 passed.
- `D:\anaconda\envs\model\python.exe -m pytest -q tests\test_sft_binidx.py --cov=src.sft_binidx --cov=data.make_sft_binidx --cov=data.tokenizer.rwkv_tokenizer --cov-report=term-missing --tb=short` -> 85 passed, total 95%; `src.sft_binidx.py` 95%, `data.make_sft_binidx.py` 98%.
- `D:\anaconda\envs\model\python.exe -m compileall -q src data tests` passed.
- `git diff --check` passed.
- `git diff -- data/SFT/sample/chat_template.jinja` was empty.

2026-06-09 Chat-template rendered converted-checkpoint demo

Completed
- Added `src.sft_prompt` for shared prompt rendering from a plain user `--prompt`.
- Updated `scripts/run_converted_rwkv_demo.py` so `--prompt` is treated as user input by default.
- The demo now internally builds a one-turn chat message and renders it with `data/SFT/sample/chat_template.jinja` before tokenization and generation. No external message JSON or message file is required.
- Added `--raw-prompt` for the old plain next-token continuation behavior.
- Added optional `--system-prompt`, `--current-date`, `--current-location`, `--enable-thinking`, and `--no-add-thinking` prompt-rendering controls.
- Updated `scripts/test_converted_checkpoint_equivalence.py` to use the same chat-template-rendered prompt behavior for forward equivalence checks.
- Updated README / README_CN converted-checkpoint inference and equivalence commands.

Validation completed
- `D:\anaconda\envs\model\python.exe -m pytest -q tests\test_sft_prompt.py --cov=src.sft_prompt --cov-report=term-missing --tb=short` -> 6 passed, `src.sft_prompt.py` 100% coverage.
- `D:\anaconda\envs\model\python.exe -m pytest -q tests\test_run_converted_rwkv_demo.py tests\test_converted_checkpoint_equivalence.py tests\test_sft_prompt.py tests\test_convert_deepspeed_checkpoint_to_pth.py --tb=short` -> 27 passed.
- `D:\anaconda\envs\model\python.exe scripts\run_converted_rwkv_demo.py --help` passed and shows `--prompt`, `--chat-template`, `--raw-prompt`; no message file input.
- `D:\anaconda\envs\model\python.exe scripts\test_converted_checkpoint_equivalence.py --help` passed and shows the same prompt-rendering controls.
- `D:\anaconda\envs\model\python.exe -m compileall -q src scripts tests` passed.
- `git diff --check` passed with only repository line-ending warnings.
- `git diff -- data/SFT/sample/chat_template.jinja` was empty.

2026-06-09 SFT preprocessing progress bar

Completed
- Added a CLI progress bar for `data/make_sft_binidx.py`.
- The progress bar refreshes one stderr line with current file, processed/total samples, remaining samples, and samples/s.
- Added `--progress/--no-progress` and `--progress-interval`.
- Kept progress output centralized in the CLI main thread; worker threads never print directly.
- Core `src.sft_binidx.build_binidx_dataset` now accepts an optional progress callback and remains silent by default for library callers.
- Updated README / README_CN parameter docs.

Validation completed
- `conda run -n model python -m pytest -q tests/test_sft_binidx.py --cov=src.sft_binidx --cov=data.make_sft_binidx --cov=data.tokenizer.rwkv_tokenizer --cov-report=term-missing` -> 79 passed, total 98%; `data.make_sft_binidx.py` 97%, `src.sft_binidx.py` 98%.
- `conda run -n model python -m pytest -q tests/test_sft_training.py tests/test_deepspeed_checkpointing.py tests/test_sft_cuda_smoke.py::test_build_tiny_sft_binidx_smoke_input tests/test_sft_cuda_smoke.py::test_cuda_sft_checkpoint_forward_backward` -> 43 passed, 1 skipped locally because CUDA smoke is gated.
- `bash -n run_13b_sft_zero3_offload.sh` passed.
- `git diff --check` passed with only repository line-ending warnings.
- `git diff -- data/SFT/sample/chat_template.jinja` was empty.

2026-06-09 13.3B SFT launcher operation docs

Completed
- Added a standalone `run_13b_sft_zero3_offload.sh` operation section to README / README_CN.
- Documented 13.3B script parameter groups:
- model shape and LoRA dimensions;
- SFT data prefix and ctx length contract;
- batch size, gradient accumulation, manual schedule, and `SFT_ONE_PASS`;
- LR/WSD, warmup, weight decay, activation checkpointing, DeepSpeed strategy, checkpoint retention, wandb, and distributed/runtime env vars.
- Added directly runnable commands for:
- preparing SFT binidx data;
- computing manual `EPOCH_STEPS`;
- launching manual multi-pass 13.3B SFT;
- launching automatic one-pass 13.3B SFT;
- resuming from `rwkv-step-N.pth`;
- converting DeepSpeed/ZeRO shards to a single `.pth`;
- verifying converted checkpoint equivalence;
- running a converted-checkpoint inference smoke test.

Validation completed
- `bash -n run_13b_sft_zero3_offload.sh` passed.
- `git diff --check` passed with only repository line-ending warnings.
- `git diff -- data/SFT/sample/chat_template.jinja` was empty.

2026-06-09 SFT README command refresh

Completed
- Updated README / README_CN with the latest SFT training behavior:
- direct manual `train.py` command with explicit `--epoch_steps` / `--epoch_count`;
- direct one-pass `train.py --sft_one_pass 1` command where `epoch_steps/epoch_count` defaults are overridden;
- 13.3B launcher commands for both manual multi-pass scheduling and automatic one-pass scheduling;
- latest gradient accumulation, DP/ZeRO accumulation equivalence, and DeepSpeed pth merge smoke commands and observed results.
- Clarified `run_13b_sft_zero3_offload.sh` comments: when `SFT_ONE_PASS=1`, `EPOCH_STEPS/EPOCH_COUNT` are integer placeholders and `train.py` overrides them from `DATA_FILE.idx`.

Validation completed
- `bash -n run_13b_sft_zero3_offload.sh` passed.
- `git diff --check` passed with only repository line-ending warnings.
- `git diff -- data/SFT/sample/chat_template.jinja` was empty.

2026-06-09 SFT WSD LR scheduler

Completed
- Added SFT-only LR WSD final-decay knobs:
- `--lr_wsd_decay_iters` controls how many final optimizer steps decay from `lr_init` to `lr_final`.
- `--lr_wsd_decay_style` supports `none`, `linear`, and `cosine`.
- Default `lr_wsd_decay_iters=0` preserves existing SFT behavior: warmup then constant `lr_init`.
- The new SFT schedule is independent from `my_exit_tokens` and does not trigger the pretraining token-limit exit path.
- Wired `LR_WSD_DECAY_ITERS` and `LR_WSD_DECAY_STYLE` into `run_13b_sft_zero3_offload.sh`.
- Updated README / README_CN SFT examples and 13.3B launch commands.

Validation completed
- `conda run -n model python -m pytest -q tests/test_sft_training.py tests/test_deepspeed_checkpointing.py --cov=src.lr_schedule --cov=train --cov=src.dataset --cov=src.sft_loss --cov-report=term-missing` -> 41 passed, total 99%, `src.lr_schedule.py` 100%.
- `conda run -n model python -m pytest -q` -> 132 passed, 6 skipped.
- `bash -n run_13b_sft_zero3_offload.sh` passed.
- `git diff --check` passed with only repository line-ending warnings.
- `git diff -- data/SFT/sample/chat_template.jinja` was empty.

2026-06-09 SFT WSD LR resume follow-up

Completed
- Clarified WSD LR decay interval in README / README_CN:
- `total_steps = epoch_steps * epoch_count`;
- decay starts at 0-based optimizer step `total_steps - min(lr_wsd_decay_iters, total_steps)`;
- epoch/step position is derived by integer division and modulo with `epoch_steps`;
- DeepSpeed/Lightning resume restores `trainer.global_step`, so WSD LR continues on the same curve when the schedule knobs are unchanged.
- Added CPU callback coverage for resumed `global_step` keeping the correct WSD decay position.
- Added optional server smoke `test_train_py_sft_deepspeed_resume_keeps_wsd_lr_position`.
- The WSD resume smoke now uses a larger default shape: `epoch_steps=33`, `warmup_steps=4`, `lr_wsd_decay_iters=9`, and `save_at_step=28`.
- This means step 0-3 warm up, step 4-23 hold `lr_init`, step 24 enters WSD decay, step 28 reaches the half-decay point and saves the checkpoint, and resume continues to step 32 where LR reaches `lr_final`.

Validation completed
- `conda run -n model python -m pytest -q tests/test_sft_training.py tests/test_deepspeed_checkpointing.py --cov=src.lr_schedule --cov=train --cov=src.dataset --cov=src.sft_loss --cov-report=term-missing` -> 42 passed, total 99%, `src.lr_schedule.py` 100%.
- `conda run -n model python -m pytest -q tests/test_sft_cuda_smoke.py::test_train_py_sft_deepspeed_resume_keeps_wsd_lr_position` -> 1 skipped locally as expected without `RWKV_RUN_TRAIN_PY_SFT_WSD_RESUME_SMOKE=1`.
- `conda run -n model python -m pytest -q` -> 133 passed, 7 skipped.
- `bash -n run_13b_sft_zero3_offload.sh` passed.
- `git diff --check` passed with only repository line-ending warnings.
- `git diff -- data/SFT/sample/chat_template.jinja` was empty.

2026-06-08 SFT best-fit packing shard groups

Completed
- Added `--pack-shard-group-size` for `best-fit-decreasing` packing.
- Default remains `1`, preserving the previous one-JSONL-at-a-time memory profile.
- Larger values group multiple JSONL shards, read them concurrently with `--num-workers`, best-fit pack across the group, and append all groups into one token binidx plus mask sidecar.
- Added regression coverage for grouped multi-JSONL best-fit packing and CLI parsing.
- Clarified README / README_CN concurrency semantics: groups are serial, reads within one group are bounded by `min(group_size, num_workers, files_in_group)`, and output writes remain single-threaded appends.
- Expanded the 13.3B SFT README / README_CN example around `run_13b_sft_zero3_offload.sh`, including data preparation, `EPOCH_STEPS` calculation, activation checkpointing, the absence of gradient accumulation, and key environment variables.
- Removed `--my_exit_tokens 0` from the generic SFT training example and clarified `--accelerator gpu` / `--devices N` single-node multi-GPU DeepSpeed behavior.

Current status
- Branch `new_sft` created from `ops-train`.
- Mainline training path currently only supports pretrain with `binidx` data and full-token loss.
- Do not use the local `sft` branch as an implementation source because the target design is different.
- Reference implementation available in `_reference_rwkv_infctx_trainer`.

Current assessment
- `train.py` asserts `data_type == "binidx"` and wires a fixed-length pretrain dataloader only.
- `src/dataset.py` only returns `(x, y)` sampled from `MMapIndexedDataset` using the magic-prime pretrain schedule.
- `src/model.py` training loss is full-token CE via fused CUDA kernels, with no selective mask input.
- Therefore, this branch is effectively pretrain-only today.

2026-06-03 SFT binidx preprocessing progress

Completed in this round
- Added `src/sft_binidx.py` to preprocess message-style SFT JSONL into token `binidx` plus a sidecar mask dataset `*.mask.bin` / `*.mask.idx`.
- Added `data/make_sft_binidx.py` CLI entry for offline preprocessing before training.
- Updated `data/tokenizer/rwkv_tokenizer.py` to accept `rwkv_vocab_v20260603.txt`, including tolerant parsing for declared-length mismatches in the vocab file.
- Updated masking policy to the current rule:
- system prompt, tool schema, user text, tool outputs, assistant role prefix remain masked.
- if an assistant message is not the final assistant reply, its `content`, optional newline before `tool_calls`, `tool_calls`, and assistant end marker are preserved in text but fully masked.
- if an assistant message is the final assistant reply, its `content`, optional newline before `tool_calls`, `tool_calls`, assistant end marker, and trailing `EOD` are trainable.
- Confirmed the compact sample `data/SFT/stf_origin_example.jsonl` correctly reconstructs `data/SFT/stf_template_example.txt`, so it can be used as the long-term regression sample instead of repeatedly depending on large `tools.jsonl`.

Validation completed
- Targeted tests: `conda run -n model pytest -q tests\test_sft_binidx.py`
- Coverage on modified preprocessing modules: `conda run -n model pytest -q tests\test_sft_binidx.py --cov=src.sft_binidx --cov=data.tokenizer.rwkv_tokenizer --cov=data.make_sft_binidx --cov-report=term-missing`
- Result: `src/sft_binidx.py` 100%, `data/tokenizer/rwkv_tokenizer.py` 100%, `data/make_sft_binidx.py` 96%, total modified-surface coverage 99%.
- Regression suite: `conda run -n model pytest -q`
- Result after mask-rule update: 71 passed.

2026-06-03 vocab strictness follow-up
- Compared `rwkv_vocab_v20260603.txt` against `data/tokenizer/rwkv_vocab_v20230424.txt`.
- The first 65529 lines now match exactly; `v20260603` only adds 5 new tail entries:
- `65530 '<|im_start|>' 12`
- `65531 '<|im_end|>' 10`
- `65532 '<|endoftext|>' 13`
- `65533 '<|think|>' 9`
- `65534 '<|tool_call|>' 13`
- Fixed the 7 corrupted entries that had been accidentally written as plain spaces in `v20260603`.
- Verified that `v20260603` now has zero declared-length mismatches.
- Switched the new SFT preprocessing path to load tokenizer vocab with `strict_length=True`.
- Validation after strict mode switch:
- `conda run -n model pytest -q tests\test_sft_binidx.py` -> 36 passed
- `conda run -n model pytest -q tests\test_sft_binidx.py --cov=src.sft_binidx --cov=data.tokenizer.rwkv_tokenizer --cov=data.make_sft_binidx --cov-report=term-missing` -> 99% total coverage
- `conda run -n model pytest -q` -> 72 passed

2026-06-03 packing and padding follow-up
- Extended SFT preprocessing to support fixed-length packing at data-build time.
- Added `--pack-length` to `data/make_sft_binidx.py`.
- Packing behavior:
- each source sample is still rendered independently and ends with one appended `EOD` from preprocessing, and that `EOD` is trainable.
- packed sequences are formed by concatenating encoded samples into fixed-length chunks.
- if the final packed chunk is shorter than `pack_length`, it is padded using `EOD` token ids.
- padding `EOD` tokens always have loss mask `0`, so only the first `EOD` that terminates a real sample is trained.
- Updated `data/SFT/stf_template_example.txt` to include the sample-final `EOD`, reflecting the new data-processing-stage append rule instead of putting it in `chat_template.jinja`.
- Validation after packing support:
- `conda run -n model pytest -q tests\test_sft_binidx.py` -> 40 passed
- `conda run -n model pytest -q tests\test_sft_binidx.py --cov=src.sft_binidx --cov=data.tokenizer.rwkv_tokenizer --cov=data.make_sft_binidx --cov-report=term-missing` -> 99% total coverage
- `conda run -n model pytest -q` -> 76 passed

Next recommended work
- Wire the mask sidecar into the training dataloader so SFT can reuse the existing binidx data path.
- Add masked loss handling in the model/training step while preserving the current pretrain fast path.
- Decide how to expose pretrain vs SFT mode in config / CLI without breaking existing training scripts.

Planned work
- Add an SFT data preparation path that converts message-style JSONL into token ids + labels + loss mask.
- Reuse the existing pretrain training loop as much as possible by adding an optional `train_mode`.
- Keep pretrain behavior unchanged when `train_mode=pretrain`.
- Add masked loss support for SFT while preserving the current fused loss path for pretrain.
- Add unit tests and regression tests for new SFT code and unchanged pretrain behavior.
- Keep changes split into small commits with clear history.

Open decisions / info needed
- Confirm target SFT source format: ChatML/messages JSONL, prompt-completion JSONL, or prebuilt Arrow.
- Confirm tokenizer/vocab choice: keep current world vocab v20230424, or add ChatML-compatible special tokens like the reference repo.
- Confirm masking policy: assistant content only, or also train role prefixes / separators / EOD tokens.
- Confirm whether SFT resume must support exact mid-epoch continuation, or epoch-level resume is acceptable initially.

Implementation direction
- Prefer offline preprocessing into an Arrow/HF dataset with `input_ids`, `labels`, `loss_mask`, and `length`.
- Dataset layer returns `(x, y)` for pretrain and `(x, y, loss_mask)` for SFT.
- Model `training_step` accepts an optional mask and falls back to standard masked CE only when needed.
- Design from scratch on `new_sft`, using only the current codebase plus `_reference_rwkv_infctx_trainer` as the behavior reference.

2026-06-04 template-aligned SFT preprocessing rewrite plan

Reason for rewrite
- The current `src/sft_binidx.py` still hardcodes a Python-side copy of the chat template.
- The user has updated the chat template behavior and provided new golden references in `data/SFT/sample/my_sample.jsonl`, `data/SFT/sample/my_sample_outs.txt`, and `data/SFT/sample/final_pattern.txt`.
- The old Python replica is now divergent enough that patching it further is riskier than rewriting the SFT preprocessing around real Jinja rendering.

Target behavior to match exactly
- Rendering `data/SFT/sample/my_sample.jsonl` with the latest intended chat template must reproduce `data/SFT/sample/my_sample_outs.txt`.
- A single serialized sample must end as:
- `...<|im_end|>\n<|endoftext|>`
- When packing multiple real samples, the boundary must be:
- `...<|im_end|>\n<|endoftext|>\n<|im_start|>...`
- In other words, each real sample contributes one learned `EOD`, and if another real sample follows there is one extra separator newline after that `EOD`.
- Tail padding in the final packed chunk remains repeated `<|endoftext|>` with loss mask `0`.

Masking rules for the rewrite
- Only the final assistant block of each source sample is trainable.
- The assistant role prefix `<|im_start|>Assistant: ` remains masked.
- The trainable span must include:
- normalized final-assistant think block text,
- final assistant visible content,
- final assistant `tool_calls`,
- the final assistant closing `<|im_end|>`,
- and the sample-final `<|endoftext|>`.
- The masked span must include:
- system text,
- tool schema,
- user text,
- tool responses,
- earlier assistant outputs,
- packing separator newline,
- and padding `<|endoftext|>` tokens.

Think normalization rules to implement
- The final assistant in the processed training text should always contain think tags.
- If the final assistant content already contains `</think>`:
- prefix render for mask boundary uses `add_generation_prompt=True`, `enable_thinking=False`, `no_add_thinking=True`.
- full render uses the original final assistant content unchanged with `add_generation_prompt=False`, `enable_thinking=False`, `no_add_thinking=True`.
- If the final assistant content does not contain `</think>`:
- prefix render for mask boundary uses `add_generation_prompt=True`, `enable_thinking=False`, `no_add_thinking=False`, so the generated prefix ends with ` <think>\n\n</think>\n\n`.
- full render manually prepends `<think>\n\n</think>\n\n` to `messages[-1]["content"]`, then uses `add_generation_prompt=False`, `enable_thinking=False`, `no_add_thinking=False`.
- Malformed edge cases such as content containing only `</think>` must be covered by tests and handled consistently with the chosen template semantics.

Implementation outline
- Stop treating Python-built segments as the source of truth for SFT text formatting.
- Build a real Jinja rendering path with the same `tojson` behavior used by the sample conversion script.
- Use render-twice mask derivation:
- first render excludes the final assistant and gives the masked prefix length,
- second render includes the full sample and gives the full text,
- the trainable span starts immediately after the assistant role prefix in the second render and ends at the end of the rendered sample text.
- Tokenize the final rendered sample text once, then derive token-level loss mask from the byte-span boundary.
- Keep the existing `.bin/.idx` plus `.mask.bin/.mask.idx` storage format.
- Update packing logic so real samples are joined by learned `EOD` plus one masked separator newline, while tail padding still uses masked `EOD` only.

Validation plan
- Add golden rendering tests against `data/SFT/sample/my_sample_outs.txt`.
- Add packing golden tests against the structure shown in `data/SFT/sample/final_pattern.txt`.
- Add focused tests for:
- no-think final assistant,
- pre-existing think final assistant,
- malformed `</think>`-only final assistant,
- final assistant with empty content but non-empty `tool_calls`,
- packed sample boundary newline behavior,
- padding behavior with simultaneous packing and masking.
- After code changes, rerun:
- `conda run -n model pytest -q tests\test_sft_binidx.py`
- `conda run -n model pytest -q tests\test_sft_binidx.py --cov=src.sft_binidx --cov=data.tokenizer.rwkv_tokenizer --cov=data.make_sft_binidx --cov-report=term-missing`
- `conda run -n model pytest -q`

Checkpoint before coding
- The sample template at `data/SFT/sample/chat_template.jinja` already includes `no_add_thinking`, but the current root `chat_template.jinja` does not yet expose that branch.
- Before the rewrite starts, confirm which file path is the authoritative training template:
- either synchronize the root `chat_template.jinja` to the new sample-template behavior,
- or temporarily point preprocessing/tests at the sample template until the root template is updated.

2026-06-04 user-confirmed rewrite constraints
- The authoritative template for the rewrite is `data/SFT/sample/chat_template.jinja`.
- `data/SFT/sample/convert.py` is the reference script for how the template is rendered and how `tool_calls.arguments` should be normalized before rendering.
- `data/SFT/sample/final_pattern.txt` is the golden reference for packed multi-sample serialization.
- `data/SFT/sample/my_sample.jsonl` and `data/SFT/sample/think.jsonl` are the primary regression inputs for the new preprocessing path.
- `data/SFT/sample/my_sample_outs.txt` and `data/SFT/sample/think_out.txt` are the corresponding golden rendered outputs for those inputs.
- The final assistant terminator of each real sample must be fully trainable:
- `<|im_end|>\n<|endoftext|>`
- That means the last assistant closing `IM_END` and the sample-final `EOD` are both `mask=1`.

2026-06-04 template-aligned SFT preprocessing checkpoint
- Rewrote `src/sft_binidx.py` around real Jinja rendering from `data/SFT/sample/chat_template.jinja`.
- Left the root chat template unchanged and pointed the CLI default at the authoritative sample template path.
- Added final-assistant think normalization, render-twice mask derivation, token-level mask projection, packed-sample separator handling, and masked tail padding.
- Kept the existing token dataset plus mask sidecar file format so the next step can wire SFT into the current binidx training path.
- Validation completed:
- `conda run -n model pytest -q tests\test_sft_binidx.py --tb=short` -> 50 passed before coverage-only branch tests were added.
- `conda run -n model pytest -q tests\test_sft_binidx.py --cov=src.sft_binidx --cov=data.make_sft_binidx --cov-report=term-missing --tb=short` -> 56 passed, modified preprocessing surface 99% total coverage.
- `conda run -n model pytest -q --tb=short` -> 92 passed.
- Next recommended work remains training-side integration: dataset mask loading, optional SFT mode, and masked loss while preserving the pretrain fast path.

2026-06-05 SFT training integration start

Current status
- SFT preprocessing is considered stable enough to serve as the training input contract.
- The user provided a local 0.4B RWKV7 checkpoint for later smoke testing:
- `model/rwkv7-g1d-0.4b-20260210-ctx8192.pth`
- `model/rwkv7-g1d-0.4b-20260210-ctx8192.txt`
- The `model/` directory is local test input and should not be committed.
- User requirements for this stage:
- keep a clear `todolist.txt` history,
- commit each stable change,
- run matching unit tests, regression tests, and coverage checks after modifications,
- keep modified-code coverage at or above 95%.

Training-side TODO
- Add an explicit `data_type=sft_binidx` mode while preserving the existing `data_type=binidx` pretrain path.
- Load `PREFIX.mask` automatically, with an optional explicit mask-prefix override.
- Make the SFT dataset return `(x, y, loss_mask)` where `loss_mask` is shifted to align with next-token targets.
- Keep pretrain dataset behavior and magic-prime schedule unchanged.
- Add masked cross entropy for SFT and preserve fused CUDA CE for pretrain.
- Add unit tests for SFT dataset alignment, mask validation, and masked CE math.
- Update train CLI / README with SFT training usage after the first training-side implementation is stable.
- Then run a small smoke test with the provided 0.4B checkpoint and a tiny SFT binidx sample.

2026-06-05 SFT training integration checkpoint 1

Completed
- Added `data_type=sft_binidx` support to `src/dataset.py`.
- SFT dataset now reads token binidx plus `PREFIX.mask` sidecar by default, or an explicit `--sft_mask_file`.
- SFT dataset returns `(x, y, loss_mask)` and shifts `loss_mask` to align with next-token labels.
- Short SFT documents are padded in-memory to `ctx_len + 1` using `--sft_pad_token_id` with mask `0`; documents longer than `ctx_len + 1` raise an error.
- Added `src/sft_loss.py` masked cross entropy for SFT training.
- `src/model.py` now uses masked CE only when the batch contains a third `loss_mask` tensor; the existing pretrain fused CE path remains unchanged for `(x, y)` batches.
- `train.py` accepts `data_type=sft_binidx`, `--sft_mask_file`, and `--sft_pad_token_id`.
- `train.py` keeps magic-prime epoch scheduling only for `data_type=binidx`; SFT preserves user-provided `--epoch_steps` and `--epoch_count`.
- `src/trainer.py` now initializes `lr` from `lr_init` so SFT runs can set `my_exit_tokens=0` safely.

Validation
- `D:\anaconda\envs\model\python.exe -m pytest -q tests\test_sft_training.py tests\test_deepspeed_checkpointing.py --cov=src.dataset --cov=src.sft_loss --cov-report=term-missing --tb=short` -> 30 passed, 100% coverage for `src.dataset` and `src.sft_loss`.
- `D:\anaconda\envs\model\python.exe -m py_compile train.py src\dataset.py src\trainer.py src\model.py src\sft_loss.py` -> passed.
- `D:\anaconda\envs\model\python.exe -m pytest -q --tb=short` -> 111 passed.

Next
- Document SFT training CLI usage in README / README_CN.
- Build a tiny SFT binidx sample and run a small smoke test with the provided 0.4B checkpoint.
- Derive recommended 0.4B args from `model/rwkv7-g1d-0.4b-20260210-ctx8192.txt`.

2026-06-05 SFT training integration checkpoint 2

Completed
- Added an explicit SFT training limit rule: `data_type=sft_binidx` now sets Lightning `max_epochs` to `--epoch_count`.
- Preserved the pretrain behavior: `data_type=binidx` still keeps `max_epochs=-1` and relies on the existing token-based / checkpoint-driven flow.
- Added tests for SFT max-epoch configuration and checkpoint path helper edge cases.

Validation
- `D:\anaconda\envs\model\python.exe -m pytest -q tests\test_sft_training.py tests\test_deepspeed_checkpointing.py --cov=src.dataset --cov=src.sft_loss --cov=train --cov-report=term-missing --tb=short` -> 32 passed, 100% coverage for `src.dataset`, `src.sft_loss`, and the testable `train.py` helper surface.
- `D:\anaconda\envs\model\python.exe -m pytest -q --tb=short` -> 113 passed.

Next
- Document current SFT training usage in README / README_CN.
- Build a tiny SFT binidx sample and run a small smoke test with the provided 0.4B checkpoint if local VRAM allows.

2026-06-05 SFT training documentation progress

Completed
- Updated README / README_CN SFT preprocessing contract to say the current `sft_binidx` trainer reads the token dataset plus mask sidecar.
- Added SFT training usage with `--data_type sft_binidx`, automatic `DATA_FILE.mask` loading, explicit `--epoch_steps` / `--epoch_count` behavior, and the `ctx_len + 1` document-length rule.
- Added a 0.4B checkpoint argument example derived from `model/rwkv7-g1d-0.4b-20260210-ctx8192.txt`: `n_layer=24`, `n_embd=1024`, `dim_ffn=4096`, `vocab_size=65536`, `head_size=64`, and LoRA dimensions `64/64/32/128`.
- Added optional server-side CUDA smoke tests in `tests/test_sft_cuda_smoke.py`.
- Default pytest skips the CUDA smoke tests; server runs can enable in-process forward/backward with `RWKV_RUN_CUDA_SFT_SMOKE=1`, or full `train.py` one-step smoke with `RWKV_RUN_TRAIN_PY_SFT_SMOKE=1`.

Validation
- `D:\anaconda\envs\model\python.exe -m py_compile tests\test_sft_cuda_smoke.py` -> passed.
- `D:\anaconda\envs\model\python.exe -m pytest -q tests\test_sft_cuda_smoke.py --tb=short` -> 1 passed, 2 skipped by default CUDA env gates.
- `D:\anaconda\envs\model\python.exe -m pytest -q tests\test_sft_training.py tests\test_deepspeed_checkpointing.py tests\test_sft_cuda_smoke.py --cov=src.dataset --cov=src.sft_loss --cov=train --cov-report=term-missing --tb=short` -> 33 passed, 2 skipped, 100% coverage for `src.dataset`, `src.sft_loss`, and the testable `train.py` helper surface.
- `D:\anaconda\envs\model\python.exe -m pytest -q --tb=short` -> 114 passed, 2 skipped.

Next
- Wait for server-side CUDA smoke results if local RTX 3050 VRAM / compile time is not representative.

2026-06-05 SFT CUDA smoke follow-up

Server feedback
- `RWKV_RUN_CUDA_SFT_SMOKE=1` reached `_build_tiny_sft_binidx` and failed because the test helper did not pass the now-required `n_epoch` and `seed` keyword arguments to `build_binidx_dataset`.

Completed
- Fixed `tests/test_sft_cuda_smoke.py` to call `build_binidx_dataset(..., n_epoch=1, seed=1234, ...)`.
- Added a default non-CUDA test that builds and reads the tiny SFT binidx smoke dataset, so this helper is covered by ordinary pytest instead of only by env-gated CUDA tests.
- Added an optional `RWKV_RUN_TRAIN_PY_SFT_RESUME_SMOKE=1` test that runs `train.py`, saves `rwkv-step-1.pth`, then resumes from that step checkpoint.
- Documented multi-GPU server usage with `RWKV_SFT_SMOKE_DEVICES=8` and strategy overrides such as `RWKV_SFT_SMOKE_STRATEGY=deepspeed_stage_3`.

Validation
- `D:\anaconda\envs\model\python.exe -m py_compile tests\test_sft_cuda_smoke.py` -> passed.
- `D:\anaconda\envs\model\python.exe -m pytest -q tests\test_sft_cuda_smoke.py --tb=short` -> 2 passed, 3 skipped by default CUDA env gates.
- `D:\anaconda\envs\model\python.exe -m pytest -q tests\test_sft_training.py tests\test_deepspeed_checkpointing.py tests\test_sft_cuda_smoke.py --cov=src.dataset --cov=src.sft_loss --cov=train --cov-report=term-missing --tb=short` -> 34 passed, 3 skipped, 100% coverage for `src.dataset`, `src.sft_loss`, and the testable `train.py` helper surface.
- `D:\anaconda\envs\model\python.exe -m pytest -q --tb=short` -> 115 passed, 3 skipped.

Next
- Ask the server to rerun `RWKV_RUN_CUDA_SFT_SMOKE=1`, then run the train.py and resume smoke tests on 8 H800 GPUs.

2026-06-05 SFT training documentation and 13.3B launcher

Completed
- Documented the SFT masked-training implementation in README / README_CN:
  - offline token binidx + mask sidecar contract,
  - `data_type=sft_binidx` scheduling,
  - dataset mask loading / validation / shift / padding,
  - model training-step dispatch,
  - masked CE behavior,
  - test coverage and server smoke test meanings.
- Recorded current local and 8xH800 server validation results in README / README_CN.
- Added `run_13b_sft_zero3_offload.sh` for 13.3B SFT training.
- Derived 13.3B args from `model/rwkv7-g1f-13.3b.txt`: `n_layer=61`, `n_embd=4096`, `dim_ffn=16384`, `vocab_size=65536`, `head_size=64`, LoRA dimensions `192/192/128/384`.
- Validated the new shell launcher syntax with `bash -n run_13b_sft_zero3_offload.sh`.

2026-06-05 SFT pth merge smoke

Completed
- Added `tests/test_sft_cuda_smoke.py::test_train_py_sft_deepspeed_checkpoint_converts_to_pth`.
- The smoke can either train a tiny SFT DeepSpeed checkpoint first, or use `RWKV_SFT_MERGE_CHECKPOINT_DIR` to test an existing checkpoint directory.
- The smoke calls `scripts/convert_deepspeed_checkpoint_to_pth.py`, loads the produced single-file `.pth`, and runs `scripts/test_converted_checkpoint_equivalence.py` against the original ZeRO checkpoint.
- Documented the server commands in README / README_CN.
- Server 8xH800 validation passed:
  - `RWKV_RUN_TRAIN_PY_SFT_MERGE_SMOKE=1`
  - `RWKV_SFT_SMOKE_DEVICES=8`
  - `RWKV_SFT_SMOKE_STRATEGY=deepspeed_stage_3_offload`
  - result: `1 passed in 284.01s`.

2026-06-08 SFT gradient accumulation adaptation

Completed
- Added SFT-aware gradient accumulation scheduling.
- `--accumulate_grad_batches` is normalized to a positive integer and used to derive `effective_bsz = real_bsz * accumulate_grad_batches`.
- SFT `samples_per_epoch` now uses `epoch_steps * real_bsz * accumulate_grad_batches`, while pretrain `binidx` keeps the historical `epoch_steps * real_bsz` behavior.
- SFT dataset length now provides `epoch_steps * accumulate_grad_batches` micro-batches per epoch, so `epoch_steps` still means optimizer steps.
- SFT mid-epoch resume from `rwkv-step-N.pth` now skips `N * accumulate_grad_batches` micro-batches inside the epoch.
- 13.3B SFT launcher now exposes `ACCUMULATE_GRAD_BATCHES` and no longer passes `--my_exit_tokens 0` in the SFT command.
- README / README_CN now describe `real_bsz`, `effective_bsz`, `ACCUMULATE_GRAD_BATCHES`, the one-pass `EPOCH_STEPS` formula, and the optional smoke-test env var.

Validation
- `D:\anaconda\envs\model\python.exe -m pytest -q tests\test_sft_training.py --tb=short` -> 13 passed.
- `D:\anaconda\envs\model\python.exe -m pytest -q tests\test_sft_training.py tests\test_deepspeed_checkpointing.py tests\test_sft_cuda_smoke.py --cov=src.dataset --cov=src.sft_loss --cov=train --cov-report=term-missing --tb=short` -> 36 passed, 4 skipped, targeted training coverage 99%.
- `D:\anaconda\envs\model\python.exe -m compileall -q src tests train.py` -> passed.
- `bash -n run_13b_sft_zero3_offload.sh` -> passed.
- `D:\anaconda\envs\model\python.exe -m pytest -q --tb=short` -> 125 passed, 4 skipped.
- `git diff --check` -> passed with line-ending normalization warnings only.
- `git diff -- data\SFT\sample\chat_template.jinja` -> empty.

2026-06-08 SFT gradient accumulation loss equivalence smoke

Completed
- Added an optional server-side CUDA smoke:
  `tests/test_sft_cuda_smoke.py::test_cuda_sft_gradient_accumulation_loss_matches_large_batch`.
- The smoke builds a synthetic SFT binidx dataset with equal trainable-token counts per sample, loads a real RWKV7 checkpoint, and compares masked loss from one large batch against split micro-batches under gradient-accumulation aggregation.
- Default comparison is `micro_bsz=2, accumulate=1` versus `micro_bsz=1, accumulate=2`; `RWKV_SFT_ACCUM_EQUIV_STEPS` can increase the accumulation count.
- The smoke is gated by `RWKV_RUN_CUDA_SFT_ACCUM_EQUIV_SMOKE=1`, and can write a JSON summary through `RWKV_SFT_ACCUM_EQUIV_SUMMARY_FILE`.
- README / README_CN now include the server command and tolerance knobs.

Validation
- `D:\anaconda\envs\model\python.exe -m pytest -q tests\test_sft_cuda_smoke.py --tb=short` -> 2 passed, 5 skipped.
- `D:\anaconda\envs\model\python.exe -m py_compile tests\test_sft_cuda_smoke.py` -> passed.
- `D:\anaconda\envs\model\python.exe -m pytest -q tests\test_sft_training.py tests\test_deepspeed_checkpointing.py tests\test_sft_cuda_smoke.py --cov=src.dataset --cov=src.sft_loss --cov=train --cov-report=term-missing --tb=short` -> 36 passed, 5 skipped, targeted training coverage 99%.
- `D:\anaconda\envs\model\python.exe -m compileall -q src tests train.py` -> passed.
- `D:\anaconda\envs\model\python.exe -m pytest -q --tb=short` -> 125 passed, 5 skipped.
- `git diff --check` -> passed with line-ending normalization warnings only.
- `git diff -- data\SFT\sample\chat_template.jinja` -> empty.

2026-06-09 SFT DP/ZeRO gradient accumulation loss equivalence smoke

Completed
- Added an optional server-side train.py + DeepSpeed/ZeRO smoke:
  `tests/test_sft_cuda_smoke.py::test_train_py_sft_deepspeed_accumulation_loss_matches_large_micro_batch`.
- The smoke builds a synthetic equal-length SFT binidx dataset and runs two real DeepSpeed training jobs with the same effective batch:
  `micro_bsz=2, accumulate=1` and `micro_bsz=1, accumulate=2`.
- The test compares the first epoch loss written to `train_log.txt`, and can write a JSON summary via `RWKV_SFT_DP_ZERO_ACCUM_EQUIV_SUMMARY_FILE`.
- The smoke is gated by `RWKV_RUN_TRAIN_PY_SFT_DP_ZERO_ACCUM_EQUIV_SMOKE=1`, requires `RWKV_SFT_SMOKE_DEVICES >= 2`, and expects a DeepSpeed strategy such as `deepspeed_stage_3_offload`.
- README / README_CN now include the server command and tolerance knobs.

Validation
- `D:\anaconda\envs\model\python.exe -m pytest -q tests\test_sft_cuda_smoke.py --tb=short` -> 2 passed, 6 skipped.
- `D:\anaconda\envs\model\python.exe -m compileall -q src tests train.py` -> passed.
- `D:\anaconda\envs\model\python.exe -m pytest -q tests\test_sft_training.py tests\test_deepspeed_checkpointing.py tests\test_sft_cuda_smoke.py --cov=src.dataset --cov=src.sft_loss --cov=train --cov-report=term-missing --tb=short` -> 36 passed, 6 skipped, targeted training coverage 99%.
- `D:\anaconda\envs\model\python.exe -m pytest -q --tb=short` -> 125 passed, 6 skipped.
- `git diff --check` -> passed with line-ending normalization warnings only.
- `git diff -- data\SFT\sample\chat_template.jinja` -> empty.

2026-06-09 SFT one-pass schedule option

Completed
- Added `--sft_one_pass` for `data_type=sft_binidx`.
- When enabled, `train.py` reads the token binidx index to count documents, computes `epoch_steps = ceil(num_documents / effective_bsz)`, and sets `epoch_count = 1`.
- The option is SFT-only and leaves pretrain `binidx` magic-prime scheduling unchanged.
- Startup logs now include the SFT one-pass document count, computed steps, and repeated tail sample count caused by ceil rounding.
- `run_13b_sft_zero3_offload.sh` now exposes `SFT_ONE_PASS`.
- README / README_CN document the option and the 13.3B launcher usage.

Validation
- `D:\anaconda\envs\model\python.exe -m pytest -q tests\test_sft_training.py --tb=short` -> 17 passed.
- `D:\anaconda\envs\model\python.exe -m py_compile train.py tests\test_sft_training.py tests\test_sft_cuda_smoke.py` -> passed.
- `bash -n run_13b_sft_zero3_offload.sh` -> passed.
- `D:\anaconda\envs\model\python.exe -m pytest -q tests\test_sft_training.py tests\test_deepspeed_checkpointing.py tests\test_sft_cuda_smoke.py --cov=src.dataset --cov=src.sft_loss --cov=train --cov-report=term-missing --tb=short` -> 40 passed, 6 skipped, targeted training coverage 99%.
- `D:\anaconda\envs\model\python.exe -m pytest -q --tb=short` -> 129 passed, 6 skipped.
- `git diff --check` -> passed with line-ending normalization warnings only.
- `git diff -- data\SFT\sample\chat_template.jinja` -> empty.

2026-06-05 SFT sample-preserving packing

Completed
- Confirmed training `ctx_len`, preprocessing `--ctx-len`, `--pack-length`, and `--pad-length` are token counts.
- Added `--ctx-len` plus boolean `--pack` / `--pad`; these use `ctx_len + 1` for next-token SFT documents.
- Kept legacy explicit `--pack-length` / `--pad-length` for compatibility.
- Changed SFT packing to preserve source sample boundaries: samples are never split across packed documents.
- Added post-tokenization length filtering before packing/padding; overlong samples are dropped instead of stopping the build.
- Updated README / README_CN and the 13.3B launcher comment.
- Validation:
  - `D:\anaconda\envs\model\python.exe -m pytest -q tests\test_sft_binidx.py` -> 71 passed.
  - `D:\anaconda\envs\model\python.exe -m pytest -q tests\test_sft_training.py tests\test_sft_cuda_smoke.py tests\test_convert_deepspeed_checkpoint_to_pth.py tests\test_converted_checkpoint_equivalence.py` -> 25 passed, 4 skipped.
  - `D:\anaconda\envs\model\python.exe -m compileall -q src data tests train.py` -> passed.

2026-06-05 SFT length-aware packing

Completed
- Added optional `--pack-strategy best-fit-decreasing` for SFT preprocessing.
- Default remains `ordered`, preserving the existing sample order after shuffle / no-shuffle.
- `best-fit-decreasing` sorts tokenized samples by length and uses a best-fit approximation to reduce padding while still never splitting a sample.
- Updated README / README_CN and the 13.3B launcher comment.

Validation
- `D:\anaconda\envs\model\python.exe -m pytest -q tests\test_sft_binidx.py` -> 72 passed.
- `D:\anaconda\envs\model\python.exe -m pytest -q tests\test_sft_training.py tests\test_sft_cuda_smoke.py tests\test_convert_deepspeed_checkpoint_to_pth.py tests\test_converted_checkpoint_equivalence.py` -> 25 passed, 4 skipped.
- `D:\anaconda\envs\model\python.exe -m compileall -q src data tests train.py` -> passed.
- Coverage for `src.sft_binidx` + `data.make_sft_binidx`: total 97%, `src.sft_binidx.py` 99%.

2026-06-05 SFT fixture tracking follow-up

Server feedback
- Server checkout did not contain `data/SFT/sample/chat_template.jinja`, because `data/*` in `.gitignore` ignored the local SFT sample fixtures.
- CUDA smoke and full SFT preprocessing regression tests require the authoritative sample template and small golden files.

Completed
- Force-added the existing local SFT test fixtures without changing their content:
- `data/SFT/sample/chat_template.jinja`
- `data/SFT/sample/my_sample.jsonl`
- `data/SFT/sample/my_sample_outs.txt`
- `data/SFT/sample/think.jsonl`
- `data/SFT/sample/think_out.txt`
- `data/SFT/sample/final_pattern.txt`
- `data/SFT/tools.jsonl`

Next
- Validate default tests after fixture tracking.
- Ask the server to rerun the CUDA smoke tests from a fresh checkout.

Validation
- `D:\anaconda\envs\model\python.exe -m pytest -q tests\test_sft_cuda_smoke.py tests\test_sft_binidx.py --tb=short` -> 68 passed, 3 skipped.
- `D:\anaconda\envs\model\python.exe -m pytest -q tests\test_sft_binidx.py --cov=src.sft_binidx --cov=data.make_sft_binidx --cov=data.tokenizer.rwkv_tokenizer --cov-report=term-missing --tb=short` -> 66 passed, 99% total preprocessing coverage.

Next
- Ask the server to rerun the CUDA smoke tests from a fresh checkout.

2026-06-05 SFT best-fit packing shard memory follow-up

Completed
- Changed only the `best-fit-decreasing` SFT packing path to process one normalized JSONL shard at a time.
- Each JSONL shard is loaded, epoch-expanded, optionally shuffled, rendered/tokenized, length-filtered, and best-fit packed independently.
- Packed documents from all shards are appended into the same final output prefix, so the result remains one token binidx pair plus one mask binidx pair:
- `PREFIX.bin`
- `PREFIX.idx`
- `PREFIX.mask.bin`
- `PREFIX.mask.idx`
- The default `ordered` packing path still uses the previous global source order behavior.
- Added a regression test where global best-fit would produce fewer docs than shard-local best-fit, proving the new path does not materialize or optimize across JSONL shards.
- Updated README / README_CN to describe shard-local best-fit behavior and the merged final output contract.

Validation
- `D:\anaconda\envs\model\python.exe -m pytest -q tests\test_sft_binidx.py --tb=short` -> 73 passed.
- `D:\anaconda\envs\model\python.exe -m pytest -q tests\test_sft_training.py tests\test_sft_cuda_smoke.py tests\test_convert_deepspeed_checkpoint_to_pth.py tests\test_converted_checkpoint_equivalence.py --tb=short` -> 25 passed, 4 skipped.
- `D:\anaconda\envs\model\python.exe -m pytest -q tests\test_sft_binidx.py --cov=src.sft_binidx --cov=data.make_sft_binidx --cov=data.tokenizer.rwkv_tokenizer --cov-report=term-missing --tb=short` -> 73 passed, 98% total preprocessing coverage, `src.sft_binidx.py` 99%.
- `D:\anaconda\envs\model\python.exe -m compileall -q src data tests train.py` -> passed.
- `git diff --check` -> passed with line-ending normalization warnings only.
- `git diff -- data/SFT/sample/chat_template.jinja` -> empty.
